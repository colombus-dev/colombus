export const EDITOR_LANGUAGE_ID = "Canopus";
export const DEFAULT_DSL_CODE = "# Create a new pattern below\n";

export const enum PATH {
    INDEX = "/",
    HOME = "/home",
    EXPLORER = "/explorer",
}